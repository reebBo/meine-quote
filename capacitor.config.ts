import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.quote.app',
  appName: ' daily-quote',
 webDir: 'dist/meine-quote/browser',
};

export default config;
